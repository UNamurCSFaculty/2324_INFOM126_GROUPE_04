name: Load Testing Action
on:
  pull_request:
    branches:
      - "release**"

jobs:
  load_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
