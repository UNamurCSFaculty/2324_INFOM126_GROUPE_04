name: 'Intergation Tests'
description: 'Run integration tests on the Django codebase'
runs:
  using: 'composite'
  steps:
    - name: Run tests
      run: ./mysite/manage.py test guestbook.integration_tests
      shell: bash
